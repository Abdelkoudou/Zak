# ============================================================================
# Supabase Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# ⚠️ NEVER commit .env to version control!

# Get these from your Supabase project dashboard:
# Settings → API → Project URL
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Settings → API → Project API keys → anon public
# This key is safe to use in client-side code (protected by RLS)
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 
# 1. The anon key is designed to be public - it's protected by Row Level Security
# 2. NEVER expose the service_role key in mobile apps
# 3. All sensitive operations should go through RLS-protected queries
# 4. For EAS builds, set these as secrets in your Expo account:
#    - Go to https://expo.dev
#    - Project Settings → Secrets
#    - Add EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY
#
# ============================================================================
