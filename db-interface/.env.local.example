# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Get these from your Supabase project dashboard:
# Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Settings → API → Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (for server-side operations like webhooks)
# Settings → API → Project API keys → service_role (secret)
# ⚠️ SECURITY: Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# Chargily Pay Configuration (Online Payments)
# ============================================================================
# Get these from your Chargily Pay Dashboard: https://pay.chargily.com
# Developers Corner → API Keys

# Secret Key (starts with test_sk_ for test mode, sk_ for live mode)
# ⚠️ SECURITY: Keep this secret! Never expose in client-side code
CHARGILY_SECRET_KEY=test_sk_your-secret-key-here

# Webhook Secret - REQUIRED IN PRODUCTION
# Used to verify webhook signatures from Chargily
# Get this from Chargily Dashboard → Webhooks → Your webhook → Secret
# ⚠️ SECURITY: This is REQUIRED in production for secure webhook verification
CHARGILY_WEBHOOK_SECRET=your-webhook-secret-here

# App URL (for success/failure redirects)
# Development: http://localhost:3005
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3005

# ============================================================================
# Environment
# ============================================================================
# Set to 'production' in production environment
# This enables strict security checks (e.g., required webhook signature)
NODE_ENV=development

# ============================================================================
# SECURITY CHECKLIST FOR PRODUCTION
# ============================================================================
# Before deploying to production, ensure:
# 1. ✅ CHARGILY_SECRET_KEY uses live key (sk_...) not test key (test_sk_...)
# 2. ✅ CHARGILY_WEBHOOK_SECRET is set (required for webhook verification)
# 3. ✅ NODE_ENV=production
# 4. ✅ NEXT_PUBLIC_APP_URL points to your production domain
# 5. ✅ All secrets are stored securely (not in git)
# 6. ✅ HTTPS is enabled on your domain

# Note: Never commit .env.local to git!
# The .env.local file is already in .gitignore
